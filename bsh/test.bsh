#Testing 1 2 3 4

#!/bin/bash

# This script saves a listing of the latest directory within a variable and the 
# prints the output stored in the variable to the screen

# -t sorts by modification time
# -d lists only directories
 
BACKUPDIR=$(ls -td * | head -1) # ls -td -1 | sed '1q' would also work
echo $BACKUPDIR
